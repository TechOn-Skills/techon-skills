export enum MagicLinkStatus {
    IDLE = "idle",
    VERIFYING = "verifying",
    VERIFIED = "verified",
    ERROR = "error",
}

export enum ResponseStatus {
    SUCCESS = "success",
    ERROR = "error",
    INFO = "info",
}